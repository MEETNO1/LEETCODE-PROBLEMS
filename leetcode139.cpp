#include<iostream>
using namespace std;

int main(){
    bool wordbreak(string, vector<string>& wordDict){
        unordered_set<string> st(wordDict.begin(), wordDict.end());
        int n = s.lenght();
        vector<bool> dp(n + 1, false);
        dp[0]= true;

        for( int i = 0; i <=n; i++){
            for( int j = 0; j <= n; j++){
              
                
            }
        }

    }
}