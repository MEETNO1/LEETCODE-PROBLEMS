#include<iostream>
using namespace std;
Struct Node{
    class Solution{
        public:
    

TreeNode* root( int root, int val){
    (TreeNode* root->left == nullptr && TreeNode*root == nullptr)
    return true;
    (TreeNode* root->left == nullptr || TreeNode*root == nullptr)
    return false;
}
TreeNode*root( int root ,int val){
   isMirror( TreeNode* right->right == TreeNode* left->left);
   isMirror(TreeNode* left->right == TreeNode*right->left);
   return true;
}
    }
};