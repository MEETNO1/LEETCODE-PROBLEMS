#include <bit/stc++.h>;
using namespace std;

int main(){
    int n ;
     cin >> n;
     dp[0] = 0;
     for(int i=1;i<=n; i++){
     while(x > 0){
        
vector<int> dp(n+1, 1e9);
dp[0] = 0;
(int c : coins) 
for (int s = 1; s <= c; i++){
  if( s- c >= 0) {
    dp[s] = min(dp[s] ,  1 + dp[s-c])
  
  }
  
     }
}
dp[s] = 1e9;
 return ;
}